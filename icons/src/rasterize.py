"""Deprecated PIL redraw — use rasterize.mjs (SVG via resvg) instead.

  cd icons/src && npm install && npm run rasterize
"""
raise SystemExit(
    "icons/src/rasterize.py is retired. Run: cd icons/src && npm install && npm run rasterize"
)
